---
- name: Configuration Template
  hosts: default
  gather_facts: false

  tasks:
    - name: Configuration template
      cisco.ios.ios_config:
        lines:
          - interface GigabitEthernet0
          - description netdevops template
          - end
